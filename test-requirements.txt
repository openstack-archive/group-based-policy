# The order of packages is significant, because pip processes them in the order
# of appearance. Changing the order has an impact on the overall integration
# process, which may cause wedges in the gate later.

setuptools>=19.2
-e git+https://github.com/openstack/neutron.git@stable/liberty#egg=neutron
-e git+https://github.com/noironetworks/apic-ml2-driver.git@liberty#egg=apic_ml2
-e git+https://git.openstack.org/openstack/neutron-vpnaas@stable/liberty#egg=neutron-vpnaas
-e git+https://git.openstack.org/openstack/neutron-lbaas@stable/liberty#egg=neutron-lbaas
-e git+https://git.openstack.org/openstack/neutron-fwaas@stable/liberty#egg=neutron-fwaas
hacking>=0.9.2,<0.10

cliff>=1.14.0 # Apache-2.0
coverage>=3.6
fixtures>=1.3.1
httplib2>=0.7.5
mock>=1.2
python-subunit>=0.0.18
requests-mock>=0.6.0 # Apache-2.0
sphinx!=1.2.0,!=1.3b1,<1.3,>=1.1.2
ordereddict
testrepository>=0.0.18
testtools>=1.4.0
testresources>=0.2.4
testscenarios>=0.4
WebTest>=2.0
oslotest>=1.10.0 # Apache-2.0
os-testr>=0.1.0
tempest-lib>=0.8.0
ddt>=0.7.0
pylint==1.4.4 # GNU GPL v2
reno>=0.1.1 # Apache2

# Since version numbers for these are specified in
# https://git.openstack.org/cgit/openstack/requirements/plain/upper-constraints.txt,
#  they cannot be refernced as GIT URLs
python-heatclient
python-keystoneclient

-e git+https://github.com/noironetworks/python-opflex-agent.git@stable/liberty#egg=python-opflexagent-agent
-e git+https://git.openstack.org/openstack/python-group-based-policy-client@stable/liberty#egg=gbpclient
